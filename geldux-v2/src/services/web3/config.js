/**
 * Chain + contract configuration for Base Sepolia.
 * Single source of truth — imported by all other service modules.
 */
export { CHAIN_ID, CHAIN_HEX, RPC_LIST, HERMES_URL, PERMIT_DEADLINE_SECONDS } from '@/config/chain'
export { ADDRESSES } from '@/config/contracts'
export { MARKETS, ASSET_KEYS, PYTH_IDS } from '@/config/markets'
