/**
 * Wallet / provider utilities.
 * Thin re-export of the shared wallet state from useWallet hook internals.
 */
export { getSigner, getAccount, getProvider, getReadProvider } from '@/hooks/useWallet'
